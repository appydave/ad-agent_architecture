# frozen_string_literal: true

RSpec.describe Ad::AgentArchitecture do
  it "has a version number" do
    expect(Ad::AgentArchitecture::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
